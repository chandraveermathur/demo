name: Execute .exe File

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Execute .exe
      run: ${{GITHUB_WORKSPACE}}\Calculator.exe
